
  function AddEventPopup() {
    var dialog = document.getElementById('EventPopup');
    dialog.showModal();
  }
  function closeEventPopup() {
    var dialog = document.getElementById('EventPopup');
    dialog.close();
  }